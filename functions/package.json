{
  "name": "coopledger-functions",
  "description": "Cloud Functions CoopLedger (alertes vote via Resend)",
  "engines": {
    "node": ">=18"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "resend": "^4.0.1"
  }
}
